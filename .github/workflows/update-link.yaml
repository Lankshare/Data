name: Update or Delete Link from Issue

permissions:
  contents: write
  issues: write

on:
  issues:
    types: [labeled]

jobs:
  update-friend:
    if: contains(github.event.issue.labels.*.name, 'update')
    runs-on: ubuntu-latest
    environment: friend-link-review
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run script
        id: run_script
        env:
          ISSUE_USER: ${{ github.event.issue.user.login }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: node .github/scripts/update-link.js

      # Pull latest changes before committing to avoid rebase conflicts
      - name: Pull latest changes
        if: steps.run_script.outputs.outcome == 'success'
        run: git pull --rebase origin main

      - name: Commit & Push
        if: steps.run_script.outputs.outcome == 'success'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(link): updated/deleted @${{ github.event.issue.user.login }}"
            git push origin main
          fi

      - name: Close issue on success
        if: steps.run_script.outputs.outcome == 'success'
        uses: peter-evans/close-issue@v2
        with:
          comment: ${{ steps.run_script.outputs.message || 'Successfully processed this request.' }}

      - name: Comment on failure
        if: steps.run_script.outputs.outcome == 'failure'
        uses: peter-evans/create-or-update-comment@v3
        with:
          issue-number: ${{ github.event.issue.number }}
          body: ${{ steps.run_script.outputs.message || 'Failed to process the request. Please check the issue details.' }}
